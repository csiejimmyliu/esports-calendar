{
  "fixture": "rest_getSchedule.json",
  "source": "riot-rest-lol",
  "request": {
    "method": "GET",
    "url": "https://esports-api.lolesports.com/persisted/gw/getSchedule",
    "queryParams": { "hl": "zh-TW" },
    "headers": {
      "x-api-key": "<public static key, see .env.example RIOT_ESPORTS_API_KEY>"
    }
  },
  "capturedOn": "2026-08-09",
  "contents": "80 events across 21 leagues, 2026-08-06T21:00Z to 2026-08-11T17:00Z",
  "verification": {
    "hl": "verified 2026-08-09 by re-requesting both locales: hl=zh-TW returns blockName \"第11週\", hl=en-US returns \"Week 11\". This fixture contains the former.",
    "capturedOn": "inferred from the event date range; not recorded at capture time"
  },
  "knownDiscrepancy": "Captured under hl=zh-TW, but src/sources/riot/rest/client.ts pins hl=en-US as the identity locale. Only display fields differ (blockName, and league.region in getLeagues), so the parser tests are unaffected — but a future assertion on either field will disagree with live. Not refreshed: this capture happens to contain two unstarted LCK matches that make the Stage 0 acceptance case exact."
}
