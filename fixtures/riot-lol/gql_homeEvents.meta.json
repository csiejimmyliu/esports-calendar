{
  "fixture": "gql_homeEvents.json",
  "source": "riot-gql-lol",
  "request": {
    "method": "GET",
    "url": "https://lolesports.com/api/gql",
    "queryParams": {
      "operationName": "homeEvents",
      "variables": "<urlencoded JSON — NOT RECORDED>",
      "extensions": "<urlencoded JSON containing the persisted-query sha256Hash — NOT RECORDED>"
    },
    "headers": {
      "apollographql-client-name": "<NOT RECORDED>",
      "apollographql-client-version": "<NOT RECORDED — a hardcoded frontend build id>",
      "accept": "application/graphql-response+json, application/json;q=0.9"
    }
  },
  "capturedOn": "2026-08-09",
  "contents": "5 events (trimmed from a larger response) across lit, lec, cacg, lpl. pages.newer and pages.older are both null, which is a trimming artefact and not evidence about the endpoint's pagination.",
  "verification": {
    "hl": "verified from contents — blockName \"第3週\" and tournament.name \"2026夏季賽\" mean zh-TW",
    "sha256Hash": "NOT RECORDED. This fixture cannot be re-requested. Recovering the hash and the pinned client version is Stage 0.5's first task, along with settling whether homeEvents is a global schedule or a curated homepage list.",
    "trimmed": "yes — do not use the event count to reason about the endpoint's coverage"
  },
  "recapture": {
    "capturable": false,
    "reason": "The persisted-query sha256Hash and the apollographql-client-version that produced this response were never recorded, and GraphQL was dropped from the roadmap on 2026-08-09 in favour of riot-rest-lol (see docs/sources/lolesports-rest.md). Nothing can replay this request. Kept only as the sole record of the composite team-id form {matchId}:{teamId}."
  }
}
