{
  "fixture": "getSchedule.json",
  "source": "riot-rest-val",
  "request": {
    "method": "GET",
    "url": "https://esports-api.service.valorantesports.com/persisted/val/getSchedule",
    "queryParams": { "hl": "en-US" },
    "headers": {
      "x-api-key": "<the same public static key as the LoL path>"
    }
  },
  "capturedOn": "2026-08-09",
  "contents": "80 events. Includes 2 events of type \"show\", which carry no match object and no blockName — the case the LoL fixture does not contain.",
  "verification": {
    "hl": "verified from contents — blockName values are \"Week 3\", \"Knockouts\", \"Play-Ins\"",
    "path": "verified in docs/sources/valorant.md — the title is selected by the /persisted/{gw|val}/ path segment; the domain is an alias and the sport query parameter is ignored"
  }
}
